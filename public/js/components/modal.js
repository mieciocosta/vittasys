function showModal(title,contentFn,width){const ov=h('div',{className:'modal-overlay',onClick:e=>{if(e.target===ov)ov.remove()}});const mc=h('div',{className:'modal-content'});if(width)mc.style.width=width;const hdr=h('div',{className:'modal-header'});hdr.appendChild(h('h2',null,title));hdr.appendChild(h('button',{className:'modal-close',onClick:()=>ov.remove(),innerHTML:'✕'}));mc.appendChild(hdr);const body=h('div');contentFn(body,()=>ov.remove());mc.appendChild(body);ov.appendChild(mc);document.body.appendChild(ov);return ov}
