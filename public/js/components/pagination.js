function buildPagination(pg,onChange){if(!pg||pg.pages<=1)return h('div');const{page,pages,total,limit}=pg;const s=(page-1)*limit+1,e=Math.min(page*limit,total);const w=h('div',{className:'pagination'});w.appendChild(h('div',{className:'pagination-info'},`${s}–${e} de ${total}`));const bs=h('div',{className:'pagination-btns'});const pv=h('button',{onClick:()=>onChange(page-1),innerHTML:I.chevL});if(page<=1)pv.disabled=true;bs.appendChild(pv);let sp=Math.max(1,page-2),ep=Math.min(pages,sp+4);if(ep-sp<4)sp=Math.max(1,ep-4);for(let i=sp;i<=ep;i++)bs.appendChild(h('button',{className:i===page?'active':'',onClick:()=>onChange(i)},String(i)));const nx=h('button',{onClick:()=>onChange(page+1),innerHTML:I.chevR});if(page>=pages)nx.disabled=true;bs.appendChild(nx);w.appendChild(bs);return w}
